# Build tooling refuses to write these in the repository; these entries are a
# final guard against accidental output from direct tool invocation.
/artifacts/
/build/
/dist/
/out/
/target/
/.torkitten-build/
*.deb
*.rpm

